msg: 
  Hello ...Welcome to config Server...i m again updation without restart config server.............
server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root
